# Channel Manifest Phase 1 Assessment

Date: 2026-04-24
Assessment branch: `ai/superpowers`
Review target: `channel-manifest-phase-1`
Diff baseline: `main...channel-manifest-phase-1`
Design spec: `docs/superpowers/specs/2026-04-24-channel-manifest-phase-1-analysis-design.md`
Checklist source: `docs/todo.md`

## Assessment Rules

- Status values are limited to `Completed`, `Partially completed`, `Not completed`, and `Requires decision`.
- Each row must cite repository evidence from code, tests, docs, or commits.
- Open product or architecture choices are tracked as `Requires decision`, not as implementation defects.

## Phase 1 Checklist

| Area | Checklist Item | Status | Evidence | Notes |
| --- | --- | --- | --- | --- |
| Manifest / attach validation | Control block stores layout type, capacity, alignment, fixed item size, and schema metadata | Completed | `include/xproc/shm/shm_layout.hpp` stores `layout_type`, `data_capacity`, `data_alignment`, `fixed_item_size`, and `schema_id`; `src/shm/shm_layout_manager.cpp` initializes those fields in `_init_header()` | The Phase 1 manifest fields from `docs/todo.md` are present in the shared control block, with only timestamp / flags left out for a separate decision. |
| Manifest / attach validation | Producer, consumer, and observer attach paths fail with typed layout errors | Completed | `include/xproc/shm/shm_layout_manager.hpp` defines `validate_error`; `src/shm/shm_layout_manager.cpp` returns typed mismatch codes; `include/xproc/ipc/shm_builders.hpp` throws `layout_exception` from `attach_fixed_channel(...)` / `attach_varlen_channel(...)`; `tests/layout_validate_test.cpp` exercises layout, schema, item-size, alignment, and version failures | The attach contract now fails with typed `layout_exception` codes instead of silently accepting mismatched layouts. |
| Manifest / attach validation | Default attach flows infer existing SHM size in the common case | Completed | `include/xproc/ipc/shm_builders.hpp` reads the existing mapping and fills inferred `transport_options`; `tests/api_surface_test.cpp` checks `attach_fixed_channel(path).options()` and `attach_varlen_channel(path).options()`; `tests/layout_validate_test.cpp` covers `infer_existing_shm_size` semantics | Non-creators no longer need to restate `shm_size`, and the builder attachers also recover layout type, alignment, item size, and schema metadata. |
| Manifest / attach validation | Creator timestamp and flags are either part of the v1 contract or explicitly deferred | Requires decision | `docs/todo.md` still leaves this checklist item unchecked; `include/xproc/shm/shm_layout.hpp` has only `reserved[3]` beyond the current manifest fields | The branch neither adds creator timestamp / flags nor documents an explicit deferral contract for them. |
| Manifest / attach validation | Public docs describe manifest-backed validation rather than manual low-level wiring | Completed | `README.md` and `docs/quickstart.rst` show `make_*_channel(...).create(...)` plus `attach_*_channel(...)`; both note that non-creators attach without repeating `shm_size` / `item_size`; `examples/README.md` describes the same builder workflow | The user-facing guidance now presents manifest-backed builders as the default Phase 1 path and leaves `transport_options` as an advanced escape hatch. |
| Builder ergonomics | C++ fixed and varlen builders exist for create and attach flows | Completed | `include/xproc/ipc/shm_builders.hpp` exports `make_fixed_channel`, `attach_fixed_channel`, `make_varlen_channel`, and `attach_varlen_channel` | The C++ public API includes the four happy-path builder entry points called out in `docs/todo.md`. |
| Builder ergonomics | Builder layer covers producer, consumer, and observer attach scenarios | Completed | `include/xproc/ipc/shm_builders.hpp` gives both `shm_channel_endpoints` and the attachers `open_producer()`, `open_consumer()`, and `open_observer()`; `tests/api_surface_test.cpp` exercises consumer and observer attach through the builder path | The builder surface covers all three endpoint roles without requiring callers to hand-wire `shm_size`, `create_if_missing`, `channel_type`, or `item_size`. |
| Builder ergonomics | At least one fixed and one varlen example use the builder API | Completed | `examples/fixed_channel_inprocess.cpp` uses `make_fixed_channel(...).create(...)` and `attach_fixed_channel(...)`; `examples/varlen_channel_inprocess.cpp` uses `make_varlen_channel(...).create(...)` and `attach_varlen_channel(...)`; `examples/README.md` calls both examples out explicitly | Both example tracks were updated to the builder-based Phase 1 workflow. |
| Builder ergonomics | Binding parity is either included in Phase 1 or explicitly deferred | Requires decision | `docs/todo.md` still leaves the binding-parity checklist item unchecked; `capi/xproc_c.h`, `node/index.js`, and `Python/src/python_binding.cpp` expose binding surfaces but no repo-level statement says parity is or is not part of the Phase 1 exit bar | The branch adds binding support, but the checklist decision about whether full binding parity belongs to Phase 1 remains unresolved. |
| Validation / mismatch coverage | Regression coverage exists for version mismatch and layout type mismatch | Completed | `tests/layout_validate_test.cpp` contains `LayoutValidate.VersionMismatch`, `LayoutValidate.LayoutTypeMismatch`, `LayoutValidate.VersionMismatchOnAttach`, and `LayoutValidate.ObserverRejectsLayoutTypeMismatchOnAttach` | The core mismatch classes named in `docs/todo.md` have direct regression tests. |
| Validation / mismatch coverage | Binding-level mismatch coverage exists for manifest failures | Completed | `tests/capi_smoke_test.cpp` covers `layout_type`, `fixed_item_size`, and `schema_id` binding failures; `node/test/smoke.test.ts` checks JavaScript schema-mismatch error metadata; `Python/tests/smoke_test.py` is success-path only; `tests/CMakeLists.txt` includes the C API smoke cases in `xproc_run_phase1_tests` | This meets the checklist's "at least some manifest failures" bar through shipped C API coverage, with extra Node source coverage present even though Python mismatch coverage is not yet added. |
| Validation / mismatch coverage | Fixed-channel item-size mismatch has dedicated C++ coverage | Completed | `tests/layout_validate_test.cpp` includes `LayoutValidate.FixedItemSizeMismatchOnAttach` | The dedicated C++ attach-time item-size regression test is present. |
| Validation / mismatch coverage | Alignment mismatch has dedicated C++ coverage | Completed | `tests/layout_validate_test.cpp` includes `LayoutValidate.AlignmentMismatchOnAttach` | The dedicated C++ attach-time alignment regression test is present. |
| Validation / mismatch coverage | Observer mismatch coverage exists for wrong layout type and schema or version mismatch | Completed | `tests/layout_validate_test.cpp` includes `ObserverRejectsLayoutTypeMismatchOnAttach`, `ObserverRejectsSchemaMismatchOnAttach`, and `ObserverRejectsVersionMismatchOnAttach`; `tests/capi_smoke_test.cpp` adds observer mismatch checks in the C API | The observer-specific mismatch paths requested by the checklist are covered in C++, with additional C-surface observer checks. |
| Validation / mismatch coverage | Manifest-version incompatibility contract is defined and tested | Requires decision | `docs/todo.md` leaves this checklist item unchecked; `include/xproc/shm/shm_layout_manager.hpp` and `src/shm/shm_layout_manager.cpp` only expose `version_major` / `version_minor`; `tests/layout_validate_test.cpp` tests current layout-version mismatch behavior only | The branch proves that version mismatches fail today, but it does not decide whether future manifest incompatibility should reuse the layout version fields or introduce a separate manifest version field. |
| Done gate | README, examples, and docs reflect the final Phase 1 workflow | Completed | `README.md`, `docs/quickstart.rst`, and `examples/README.md` all show the builder attach flow; `docs/building.rst` documents the focused Phase 1 regression target | The repository's user-facing docs consistently describe the builder-based attach workflow and how to run the named Phase 1 gate. |
| Done gate | A targeted Phase 1 regression suite is easy to run locally and in CI | Completed | `tests/CMakeLists.txt` defines the `xproc_run_phase1_tests` target and its regex; `README.md` and `docs/building.rst` document the command; `.github/workflows/ci.yml` runs that target on Linux and Windows | The dedicated Phase 1 suite is named, documented, and wired into CI as a first-class target. |
| Done gate | Phase 2 work does not rely on low-level attach details that Phase 1 was supposed to hide | Partially completed | The C++ happy path is builder-based in `README.md`, `docs/quickstart.rst`, and the in-process examples, but the public C / Node / Python surfaces in `capi/xproc_c.h`, `node/index.js`, and `Python/src/python_binding.cpp` still center on low-level option structs and manual attach details | The repository no longer requires low-level SHM wiring for common C++ use, but it has not eliminated those details from the broader public surface enough to close this done-gate item. |

## Summary

The branch completes the implemented Phase 1 core for the C++ shared-memory workflow: the manifest metadata is embedded in the control block, attach-time validation fails with typed layout errors, the builder API and examples are in place, the docs describe the builder-first contract, and the targeted `xproc_run_phase1_tests` gate is documented and wired into CI.

Items still open because they need more code or coverage are limited to the repository-level done gate about Phase 2 callers depending on low-level SHM attach details. The C++ happy path now hides those details, but the public C, Node, and Python surfaces still rely on low-level option wiring rather than builder-style attach abstractions.

Items still open because the repository still needs a product or architecture decision are the creator timestamp / flags question, the binding-parity-as-Phase-1-exit question, and the future manifest-version contract question about reusing layout version fields versus adding a separate manifest version field.

Phase 1 is functionally complete and documentation-complete for the implemented C++ core, but it is still materially open at the repository level until the remaining scope decisions and low-level-surface follow-up are closed.
